export enum ChatRoomEnum {
  AddedToRoom,
  AlreadyInRoom,
  Banned,
  NotInvited,
  WrongPass
}

export enum ChannelType {
  Private,
  Protected,
  Public
}

export enum ActionType {
  Ignore,
  Kick,
  Mute,
  Unmute,
  Ban,
  Promote,
  Demote
}
